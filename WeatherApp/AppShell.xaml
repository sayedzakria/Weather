<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="WeatherApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:WeatherApp"
     xmlns:views="clr-namespace:WeatherApp.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="WeatherApp"
   
       
           >
    
    <TabBar>
        <Tab Title="Home"
            Icon="home.png">
            <ShellContent ContentTemplate="{DataTemplate views:Home}" />
        </Tab>
        <Tab Title="Search" 
            Icon="add.png">
            <ShellContent ContentTemplate="{DataTemplate views:Search}" />
        </Tab>
        <Tab Title="Forecast "
     Icon="map.png">
            <ShellContent ContentTemplate="{DataTemplate views:Forecast}" />
        </Tab>
    </TabBar>
   
</Shell>
